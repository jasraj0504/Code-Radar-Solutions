#include <stdio.h>

int main() {
    int a = 7, b = 3; // Initialize the variables

    if(a > 0 && b > 0)
        printf("True\n"); // Print True if both a and b are greater than 0
    else
        printf("False\n"); // Print False otherwise

    return 0;
}
